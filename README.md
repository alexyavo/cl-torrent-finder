DHT Crawler in Common Lisp.

Used to depend on an external package named `alxcl-utils` which is copied to a directory here; might need some tinkering with the `.asd` project file definition to get working.
